# 集成验证记录

日期：2026-08-31。集成分支：`codex/product-integration`。本记录只对应本次本地软件检查，不能代替实机验收。

## 已完成

- 原脑电基线：原有 92 项测试全部通过，Python 3.12.13，Qt offscreen，测试设置转存到临时 INI。
- 冻结内容：164 个文件与来源 Git blob 一致；源清单及记录的固件规范 SHA-256 已校验。
- 校验器：4 项测试通过，覆盖文件改动、缺失、二进制、CRLF、路径越界、来源遗漏、错误提交及错误 SHA-256。
- 独立来源审查：未发现当前归档遗漏或误改。首轮发现的来源完整性和 SHA-256 检查缺口已修复并复审通过。
- STM32 归档：在其独立 `app/.venv` 按原 `uv.lock` 安装成功，原有 50 项测试通过；独立入口的设置、锁、固定 STM32/串口及 8767 SDK 实际握手检查通过。
- STM32 Qt 离线截图已人工查看，窗口正常生成；归档原版工具条较拥挤，未在本轮修改其 GUI 布局。
- 肌电：独立 `app/.venv` 按基线锁文件安装成功，101 项测试全部通过；包含 USB/BLE 采样率读回、全差分语义、软件触发、SDK、记录分段、BDF/FIF、文件重载和错误元数据保护。
- 肌电新增控件：初次截图发现采样率控件被挤出原工具栏，已移至独立工具栏并新增回归测试；1500 和 1280 宽度截图已查看。离线渲染器无系统字体时显式加载 Windows 微软雅黑，仅用于 QA，不修改应用字体配置。原界面窄窗口工具栏拥挤仍是继承限制。
- 本地全仓 `ruff check .` 通过。三套运行环境互相独立。
- 原实际运行目录 `D:/PycharmProjects/onmiBCI_activatemask` 仍在 `master`，工作区无更改；稳定 V19 `.ino` 的磁盘 SHA-256 仍为 `05DBF7EEE580AEBA1C5D459AEC68B7DE5A56365A18C221A8EB636A40B52EA387`。

## 审查范围

来源校验和 STM32 独立入口已完成第二位审查者复审。肌电适配已由主实现者进行差异检查、回归和截图核验，但第二位审查者的完整肌电复审因工具额度限制中断，尚待补齐。本次只提交本地候选版本，不把该项标记为完成，也不据此合入 `master`。

本轮回归先复现再修复的问题包括：BDF 解析器误传数组、采样率控件不可见、无效 BIN 元数据改变当前记录配置、时间轴缺口上限未随采样率改变，以及设备采样率变更错误影响已载入文件的时间尺度。

## 尚未验证

- 未对 ESP32、STM32、E73 或 Dongle 烧录固件，也未重新编译导入的固件或 HEX。
- 肌电软件与真实设备配对后的 250/500/1000 SPS、USB/BLE 吞吐、丢包与长时间记录，需硬件验收。
- STM32 的 SD、外部硬触发及后续 GUI 功能对齐，本阶段没有实现。
- 新 CI 工作流仅本地检查；尚未推送运行远端 CI。
- 不在此阶段合并 `master` 或推送远端；待用户验收后执行。
