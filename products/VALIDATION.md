# 集成验证记录

日期：2026-08-31。用户已授权完成代码复核并将三产品及打包修复统一提交、合入和推送 `master`。本记录对应软件检查，不能代替实机验收或远端 CI 结果。

## 已完成

- 原脑电基线：原有 92 项测试加 7 项打包回归，共 99 项通过；另有 54 个 subtests 通过。Python 3.12.13，Qt offscreen。稳定 GUI/固件/运行入口/依赖锁均未修改。
- 冻结内容：164 个文件通过校验；其中 5 个用户授权的打包文件按独立例外 blob 校验，其他文件与来源一致。原始源清单及固件规范 SHA-256 保留并校验。
- 校验器：5 项测试通过，覆盖文件改动、缺失、二进制、CRLF、路径越界、来源遗漏、错误提交、错误 SHA-256，以及禁止给固件添加打包例外。
- 独立来源审查：未发现当前归档遗漏或误改。首轮发现的来源完整性和 SHA-256 检查缺口已修复并复审通过。
- STM32 归档：在其独立 `app/.venv` 按原 `uv.lock` 安装成功，原有 50 项测试通过；独立入口的设置、锁、固定 STM32/串口及 8767 SDK 实际握手检查通过。
- STM32 Qt 离线截图已人工查看，窗口正常生成；归档原版工具条较拥挤，未在本轮修改其 GUI 布局。
- 肌电：独立 `app/.venv` 按基线锁文件安装成功，107 项测试及 56 个 subtests 全部通过；包含 USB/BLE 采样率读回、全差分语义、软件触发、SDK、记录分段、BDF/FIF、文件重载和错误元数据保护。
- 肌电新增控件：初次截图发现采样率控件被挤出原工具栏，已移至独立工具栏并新增回归测试；1500 和 1280 宽度截图已查看。离线渲染器无系统字体时显式加载 Windows 微软雅黑，仅用于 QA，不修改应用字体配置。原界面窄窗口工具栏拥挤仍是继承限制。
- 本地全仓 `ruff check .` 通过。三套运行环境互相独立。
- 稳定 V19 `.ino` 的磁盘 SHA-256 为 `05DBF7EEE580AEBA1C5D459AEC68B7DE5A56365A18C221A8EB636A40B52EA387`；实际运行目录保留原脑电 `.venv`、设置和记录，新增产品环境在各自 `app/.venv`。
- 脑电 EXE：只打包稳定 native 入口，不包括肌电或 STM32。独立构建环境、DLL 搜索路径隔离及 MNE 延迟模块收集已验证，完整版通过原生 Windows Qt 启动、冻结环境 BDF/FIF 写入读回。构建不删除旧输出和数据。
- 生成物：Git 当前树和已获取历史无桌面 EXE/发布压缩包，仅保留两个 STM32 固件升级 ZIP。新增忽略规则，Actions 只验证构建，不上传桌面产物。

## 审查范围

来源校验、STM32 独立入口及肌电适配均已完成第二位审查者复审。最后一轮肌电审查发现的四类问题已先用测试复现，再修复并复审：离线文件增益污染实时标定、500/1000 SPS 下 PSD 的 FFT 长度不足、阻抗流程绕过设备采样率校验、BIN 通道名错误使用当前界面名称校验。独立复审运行 27 项针对性测试，退出码为 0，未发现新的阻塞项；主实现者完整肌电测试 107 项及 56 个 subtests 通过，退出码为 0。

本轮回归先复现再修复的问题包括：BDF 解析器误传数组、采样率控件不可见、无效 BIN 元数据改变当前记录配置、时间轴缺口上限未随采样率改变，以及设备采样率变更错误影响已载入文件的时间尺度。

## 尚未验证

- 未对 ESP32、STM32、E73 或 Dongle 烧录固件，也未重新编译导入的固件或 HEX。
- 肌电软件与真实设备配对后的 250/500/1000 SPS、USB/BLE 吞吐、丢包与长时间记录，需硬件验收。
- STM32 的 SD、外部硬触发及后续 GUI 功能对齐，本阶段没有实现。
- 远端 CI 结果以 Actions 运行记录为准，本地通过不等于远端已通过。
- 软件归并到 `master` 不将肌电组合或 STM32 参考标记为已通过实机发布验收。
