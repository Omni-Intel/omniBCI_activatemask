CONFIG_MCUBOOT_LOG_LEVEL_WRN=y

# NCS 3.4.0 calls this Nordic hook unconditionally but only declares it for
# Nordic cleanup builds. Keep the workaround local to the MCUboot image.
CONFIG_COMPILER_OPT="-Dnrf_crypto_keys_housekeeping()=((void)0)"
