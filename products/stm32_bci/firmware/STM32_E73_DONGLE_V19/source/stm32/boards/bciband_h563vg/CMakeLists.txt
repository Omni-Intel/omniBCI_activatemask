# MCUboot's NCS compatibility include is applied only to the MCUboot image by
# tools/fw.ps1. Board-level compile options also run during outer sysbuild
# discovery and crash CMake 4.2.1 before the board can be configured.
