# OmniEMG V20 integration.1 runtime

Use the product's `../setup.bat` and `../run.bat`.
Hardware pairing, provenance, supported behavior and acceptance limits are in
`../README.md`. The historical dependency-project version is not the product release version.
